## Commit to a Loacl Repository
- `git status` - view status of files in the working tree and staging area
- `git add` - adds untracked or modified files to the staging area
- `git commit` - created a snapshot of the current project
- `git log` - view the commit history
- `git push` - push the commit to the remote repository